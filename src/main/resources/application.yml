server:
  port: 8081
spring: 
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/fridge?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  http:
    multipart:
      max-file-size: 10Mb
      maxRequestSize: 10Mb
  

  